name: sanity-check

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  hub-health:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Hub Status Report
        run: |
          echo "✅ Hub Sanity Check: $(date)"
          echo "-----------------------------------"
          echo "Detecting Active Silos:"
          ls -d */ | grep -v "docs" | grep -v "site"
          echo "-----------------------------------"
          echo "Check completed for ki7mt-mcp-hub."